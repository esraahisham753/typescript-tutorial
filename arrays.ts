let superHeros: string[] = [];
let heroPower: Array<number> = [];

superHeros.push("Spiderman");
heroPower.push(2);

type User = {
    name: string
    isActive: boolean
};

let allUsers: Array<User> = [];

allUsers.push({name: "esraa", isActive: false});

let MLModelInput: number[][] = [
    [255, 255, 255]
];

export {};